# 006: интеграция и переносимая поставка, 2026-09-24

Задача принята через [PR #6](https://github.com/obolibok/filesystem-mcp/pull/6).
Merge: `9707bbe311b1cf93d5b6601e166cbad8d5ad3cd1`.
Проверенный PR head: `bf0e81fd061e66de83efecbc4c60cef9e2562f31`.
Runtime совпадает с `fc031a02`, прошедшим [code review](006-review-2026-09-24.md)
и [живой опыт](006-live-2026-09-24.md). Интеграция выполнена в отдельном worktree;
checkout исполнителя не изменялся, main обновлён fast-forward после merge.

## Интеграционные исправления и проверки

- Merge с актуальным main прошёл без конфликтов. Prettier исправил только
  форматирование двух planning-документов; runtime не менялся.
- Первый Windows CI обнаружил 8 ложных отказов config fingerprint. Тестовый
  submitShared захватывал fingerprint до initialize/раскрытия 8.3 scratch path.
  Рабочий snapshot tool уже вызывает assertScratchSeparated до fingerprint;
  helper приведён к тому же порядку, проверки доступа и assertions не ослаблялись.
- Сбой воспроизведён локально с настоящим Windows 8.3 TEMP/TMP до исправления.
  После исправления весь snapshot suite в этом окружении: 38/38 PASS, 0 skips.
- Итоговый локальный npm run check: 420 tests, 412 pass, 0 fail, 8 platform/permission
  skips; build/types/ESLint/Prettier/Knip PASS.
- [CI итогового PR head](https://github.com/obolibok/filesystem-mcp/actions/runs/36058305580):
  Windows — 417 pass, 0 fail, 3 skips; Ubuntu — 412 pass, 0 fail, 8 skips.
  Оба jobs выполнили полный npm run check. Предыдущий красный CI не замалчивается:
  [воспроизведённый сбой](https://github.com/obolibok/filesystem-mcp/actions/runs/36057770614).

Живой опыт подтвердил completed/inflight reuse между ChatGPT-чатами, forced retry,
материализацию manifest/трёх ZIP с 21011 строками, hashes/CRC, сохранение старого
кеша при изменении source и forceRefresh 12 → 13 строк, затем мгновенный Ctrl+C.
Отсутствие процессов и readiness после остановки независимо проверено.

## Поставка для переноса

Чистая папка: `out/Schwarzbeck-MCP-2026-09-24`.
Рядом ZIP и файл `.zip.sha256`; выходные файлы игнорируются Git.

- BUILD.json sourceCommit: `91d4a001f4c131178c6beb7efb791be6584ca1ac` — принятый
  предок PR head. Последующий commit меняет только test helper; diff src/scripts/
  package.json/package-lock.json между сборкой и merge пуст. Пересборка одинакового
  runtime не требовалась, BUILD.json сохраняет настоящий источник сборки.
- Node 24.15.0 Windows x64, tunnel-client 0.0.14 full; архивы проверены по pins builder.
- 13 Verify.mjs scenarios PASS: перенос с Unicode/пробелами, несколько roots,
  snapshot/bundle bytes и SHA/CRC, ограничения конфигурации, PowerShell 5.1 doctor,
  отказ emergency stop по чужому PID.
- 2781 файл, 178999799 B в папке. Все файлы из SHA256SUMS.json проверены по размеру
  и SHA-256; проверено отсутствие лишних файлов и содержимого рабочего data.
- ZIP: 69208965 B; CRC всех entries PASS.
  SHA-256: `8b5e3dda6f07f1306d0511228001f526568986d2042aaececfe83f192d37df22`.
- В комплекте README.md и автономный README.html: Windows 10/11 setup, tunnel,
  runtime key, ChatGPT plugin, multiple roots, scratch/TTL, freshness/reuse/forceRefresh,
  остановка и операторский smoke. Никаких credentials или production inventories.
  Config открывает только sample-source, tunnelId пуст; рабочий live-профиль не копировался.

На целевой VM: распаковать весь ZIP, выполнить Check.ps1, настроить config.json
и пройти инструкцию. Ключ вводится скрыто при Start.ps1. Windows 11/новая VM/SMB ACL
этим прогоном не проверены. Release и смена upstream version не выполнялись.

## Следующий шаг

006 завершена. 005 остаётся proposed: предметный опыт назначается после выбора
разрешённых источников, исходного вопроса и критериев результата. Перенос комплекта
на VM и её operator smoke пользователь выполняет по вложенной инструкции.
