# Итог локального ревью 003: 2026-09-20

Результат: **локальный code review PASS**, замечания R1–R8 закрыты.
Реализация допущена к synthetic live ChatGPT проверке. Задача целиком ещё не
`done`: целевая доставка остаётся pending, интеграция в main не выполнялась.

Исполнитель: `003 - compressed snapshot`, task
`01a0be9c-0f01-7c42-8abb-a6ff550e1532`, ветка `codex/003-compressed-snapshot`.
Проверенный head: `9ff207922b55d040608590323e025fd45eb29634`.
R8 runtime/tests: `df89914f`; документация/evidence: `9ff20792`.
Planning main `aee444c3` включён обычным merge `fe7d0939`.

История: [первое ревью](003-review-2026-09-20.md),
[повторное ревью](003-review-r2-2026-09-20.md).
R1–R7 проверены в предыдущем раунде; этот diff ограничен R8 и двумя
уточнениями evidence. Новых замечаний в проверенных изменениях не обнаружено.

## Закрытие R8 и evidence

- Независимое прежнее воспроизведение с одноразовым EACCES теперь выдаёт warning;
  оба `initialize()` и `close()` успешны. Неудалённые bytes остаются учтёнными,
  expired artifact недоступен, повторная cleanup освобождает квоту однократно.
- Новые направленные tests для EACCES/EPERM и фатальной ошибки scratch: 2 pass,
  0 fail, 0 skip. Проверены доступность новой job до удаления и повторная очистка.
- Verifier сравнивает header до data rows. Независимый probe принимает корректный
  пустой CSV и отклоняет пустой CSV с неверным header.
- HTTP response-loss test теперь ожидает completed восстановленной job.
  Отдельный реальный SDK timeout (10 ms при задержке ответа 100 ms) проверяет
  recovery через тот же idempotency key и также ожидает completed.

## Независимый полный check

В изолированном review checkout точного head выполнен `npm run check`:
**380 tests, 373 pass, 0 fail, 7 прежних Windows skips**.
Build, type-check, lint, formatting и остальные static checks прошли.
Runtime/tests/dependencies не модифицировались. Lockfile не изменился с предыдущего
review, использовались ранее установленные через `npm ci` исходные зависимости;
временный tsx fallback исполнителя в этом независимом прогоне не требовался.

Исполнитель повторил pipeline benchmark после изменения verifier: 100 000 rows —
4,234 s / peak RSS 120 557 568 B; 3 000 000 rows — 126,193 s /
peak RSS 236 859 392 B, 478 889 517 raw B, 50 373 149 ZIP B, 11 частей,
verified=true. Планирование проверило отчёт, но эти большие прогоны не дублировало.
Неизменённые walk/poor-compression профили сохраняют доказательства прошлого раунда.
Все проверки synthetic; production данные не использовались.

## Следующий шаг

Пошаговый synthetic live опыт с пользователем на проверенном head по
`docs/testing/snapshot-live.md` из ветки реализации: быстрый submit, независимые
status calls, материализация manifest и минимум трёх частей, SHA-256, CRC,
CSV counts, повторная выдача и size ladder с архивом около 5,53 MB.
Локальный review PASS не доказывает доставку в ChatGPT.

Для опыта использовать task `003 - compressed snapshot` и её проверенную ветку;
при отдельном назначении нового live-чата передать ему этот head и протокол.
004 остаётся proposed до результата целевой проверки и приёмки 003.
Push, PR и merge этим review не выполнялись и не поручены.
