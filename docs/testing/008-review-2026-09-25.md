# 008: независимое code review, 25.09.2026

Результат: **PASS для подготовки тестовой поставки**. Проверенный head
`7bf58e0539054f0338bc797ab832e12e8036c023`, base
`5028c06c352d6e2753e577b8f0a16396c6bdbedb` (reviewed 007 + planning docs).
Diff: 6 файлов, +889/-39. Checkout исполнителя чистый и не изменялся.
Независимый detached review checkout: .worktrees/008-review.

## Проверенное поведение

Прочитан весь runtime diff, новые tests, reference и work record. Metadata rename
сохраняет старый destination и повторяет только известные transient ошибки rename.
На одну запись отводится 2500 ms, terminal failure получает ещё 2500 ms recovery.
Checkpoint учитывает cancel/timeout/close; записи сериализованы по job, отдельная
очередь упорядочивает terminal writers. Metadata-temp резервируется до async write
в общей quota; удаление/оставшийся orphan учтены. Новых durable sidecar нет.

Если terminal persist не проходит, live status явно показывает optional
metadataPersistence с recovering/failed и ограниченной безопасной диагностикой.
Успешное восстановление убирает поле. После постоянного отказа нет обещания durable
terminal state: restart использует последнюю пригодную metadata, running/queued
становится interrupted. Complete reuse исключает отмеченный persistence failure.
ACCESS_DENIED больше не описывается как Job failed, fatal fallback сохранён.

## Независимые проверки

- npm ci --ignore-scripts и полный npm run check: **441 tests, 433 pass,
  0 fail, 8 platform/permission skips**, Windows / Node 24.15.0.
  Static build/types/lint/format/Knip и MCP wire/tool budget PASS.
- Прежний planning reproducer повторён на исправленном head: 500 checkpoints
  и 688 status reads без блокировки завершились completed в памяти и на диске.
  Реальные Windows handles без FileShare.Delete на 50 и 1500 ms теперь также
  дают durable completed; прежде 1500 ms оставляли failed/live и running/disk.
- Отдельный независимый test удерживал настоящий Windows handle на job.json
  3500 ms только при terminal write. Наблюдалось второе окно recovering.
  Для реального snapshot с a.txt/b.pem/z.txt после release и restart получено
  completed/complete=false, 2 записи и оба artifacts доступны с проверкой hashes.
  Для synthetic ENOSPC producer сохранены failed и native ENOSPC/write после
  release/restart. В обеих metadata нет live-only поля после восстановления.
- Целевые 11 tests входят в full suite: permanent 6500 ms handle, deterministic
  EIO/EPERM, terminal-only recovery, cancel/timeout/close, quota и legacy behavior.
- git diff --check PASS; tracked review tree чистое. Первый запуск дополнительного
  planning harness остановился на обязательном idempotencyKey; harness исправлен,
  оба реальных terminal lock сценария после этого PASS. Runtime не менялся.

Actionable замечаний к этому diff не найдено. POSIX ветки этим Windows запуском
не проверены; GitHub CI и новый большой пользовательский live ещё впереди.
Окна ограничивают повторные попытки, но не зависший OS syscall. Блокировка дольше
terminal recovery window остаётся диагностируемым ограничением, а её источник
в пользовательском опыте по-прежнему неизвестен. Снижать polling не требуется.

## Передача

Пользователь разрешил после успешного review подготовить новую сборку.
Собирать из проверенного head в отдельный output, выполнить portable verifier,
добавить протокол повторного большого опыта и hashes. Установленный сервис,
его config/jobs/tunnel и source деревья не изменять. Done/merge пока не ставится.
