# Живой опыт 003: 2026-09-20

Статус: **in progress; целевой результат ещё не получен**.
Пользователь разрешил провести опыт после локальной приёмки 003.
Проверенная сборка: `9ff207922b55d040608590323e025fd45eb29634`.
[Локальное ревью](003-review-r3-2026-09-20.md) — PASS.

## Подготовка и текущий шаг

- На Windows подготовлен отдельный synthetic source root и отдельный scratch.
  Сервер запускается с `--read-only`, `--root-boundary` и общим cap 10 MiB.
  Snapshot caps: raw CSV 45 MiB, ZIP/delivery 8 MiB, scratch 1 GiB.
- Прежний tunnel profile скопирован в отдельный ignored каталог и направлен на
  принятую сборку. Старый профиль не изменён. Ключ остаётся env-only; пользователь
  подтвердил наличие ключа и введёт его локально через скрытый PowerShell prompt.
- До запуска туннель был остановлен, loopback health port 18080 свободен.
  Источники ограничены synthetic fixture; production каталоги не подключаются.
- Следующий пользовательский шаг: запустить подготовленный local starter и
  оставить окно открытым. Затем проверить readiness и обновить действия FileMCP.

## Локальный preflight

Через настоящий stdio MCP выполнены submit/reuse/conflict/status/get_artifact:
12 tools; 12 zero-byte synthetic файлов, включая Unicode, comma, space и имена
без расширения. Получены manifest 1835 B и ZIP 506 B с 12 CSV rows; независимый
CSV parse подтвердил точное множество имён. Повторные bytes совпали, изменение
параметров при том же idempotency key отклонено.

Это **LOCAL_ONLY_NOT_CHATGPT**. Данный результат не подтверждает материализацию
файлов в принимающей среде.

## План целевого опыта

1. Пройти discovery и маленький smoke snapshot с материализацией manifest/ZIP.
2. Подготовить измеренный multipart synthetic fixture: минимум три части одного
   снимка, две около 5,53 MB и меньший остаток; отдельный набор для верхней ступени
   в пределах 8 MiB ZIP cap. Размеры сначала измерить локально.
3. Получить **все** части каждого проверяемого snapshot, чтобы подтверждать общий
   count и exact expected set. Если получены только выбранные части, фиксировать
   частичный результат доставки без утверждения полноты.
4. В ChatGPT независимо проверить SHA-256, CRC, CSV schema/counts/paths и повторную
   выдачу. Base64 length отличать от полного измеренного wire size.
5. Для Windows live проверить Unicode/comma/no-extension. Quotes/newline в именах
   невозможны на Windows и остаются в synthetic metadata regression suite.
6. Записать фактические PASS/FAIL и cleanup. 004 пока не начинать.

Runbook маршрута: [Secure MCP Tunnel](https://developers.openai.com/api/docs/guides/secure-mcp-tunnels),
[подключение и проверка плагина](https://developers.openai.com/plugins/deploy/connect-chatgpt).
Локальные профили, ключ, большие fixtures и подробные логи не входят в Git.
