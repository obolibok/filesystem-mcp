# 007+008: интеграция и CI, 25.09.2026

Интеграционный [PR #7](https://github.com/obolibok/filesystem-mcp/pull/7), ветка
codex/008-integration. Runtime соответствует reviewed и пользовательской сборке
`7bf58e0539054f0338bc797ab832e12e8036c023`. Отдельный worktree объединяет 007+008
и актуальную planning-документацию. Версии пакетов не менялись.

## Первый CI и исправление тестов

[Первый run](https://github.com/obolibok/filesystem-mcp/actions/runs/36134468341)
на head `878a69ba63f70f28ff1b23bdef84700daafa2f90`:

- Windows: 441 tests, 438 pass, 0 fail, 3 skips.
- Ubuntu: 441 tests, 427 pass, 2 fail, 12 skips.

Оба Ubuntu failures в snapshot-walk-recovery.test.ts зависели от порядка
filesystem enumeration. Проверка двух соседних CSV paths требовала a,z, хотя
снимок не обещает сортировку. Fixture fatal-after-samples связывал ошибки с
именами 025..045/049, но на Linux fatal приходил до исчерпания 20 samples.

В интеграционной ветке исправлены только tests: первый сравнивает отсортированный
список; второй инъецирует 21 recoverable fault по номерам stat-вызовов 26..46,
затем fatal на 50-м, запоминая фактический путь. Проверки 20 samples, 22 errors,
закрытых частей перед fatal, удаления artifacts и fatalError/restart сохранены.
Добавлена проверка точного количества stat calls. Сервер не сортирует обход и
runtime не менялся; новая сборка для этого изменения не требуется.

Локальный полный npm run check после правки: **441 tests, 433 pass, 0 fail,
8 skips**, build/types/ESLint/Prettier/Knip PASS. git diff --check PASS.
Финальный head `6b4791e1595d36b0fdb538cc0e9dc4dd56b5d48c`:
[CI run 36135072216](https://github.com/obolibok/filesystem-mcp/actions/runs/36135072216)
**PASS** на обеих платформах. Ubuntu: 441 tests, 429 pass, 0 fail, 12 skips;
Windows: 441 tests, 438 pass, 0 fail, 3 skips.

В первой попытке этого run Windows получил один ETIMEDOUT запуска PowerShell
helper в прежнем bundle-live-harness test для коротких путей (лимит 10 s).
Проверки 007/008 прошли. Повторён только failed Windows job на том же commit,
без изменения кода; итог PASS. Причина задержки запуска helper не установлена,
она не записывается как runtime fault. Переносимая сборка пользователя остаётся
той же: исправления после неё затронули только tests и documentation.

## Статус приёмки

Code review, portable checks и [пользовательский live delivery](008-live-2026-09-25.md)
прошли. Два сохранённых результата независимо проверены: 484719 и 69779 записей,
completed/complete=false, SHA/CRC/CSV PASS. Реальный restart старого jobId и manifest
пользователь ещё не проверял; инструкция дана. До подтверждения этого шага PR
остаётся draft, done/merge не выполняется. Synthetic restart tests PASS, но их
свидетельство не подменяет отдельный live-этап.
