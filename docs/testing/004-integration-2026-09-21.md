# Приёмка live и интеграция 004: 2026-09-21

Live acceptance — **PASS**. Проверены материалы задачи
`004-live - Windows ChatGPT bundle`, task `01a0c45b-e972-71f3-9177-2801312cd01f`.
Исполнитель передал `edbe2c2f77463d0ec3e4b694595f84c4266b7da3`; запущенный
runtime соответствует reviewed `82e8d18756e1787f41379720b5c5dd6f29fe42ca`.
[Исходный live-отчёт](004-live-2026-09-21.md) содержит jobs, artifacts и hashes.

## Независимая сверка планированием

Прочитаны три клиентских JSON и статически проверены присланные verifier scripts;
код из пользовательских файлов локально не исполнялся. Собственный bounded verifier
прочитал сохранённые server ZIP/manifest, распаковал entries в памяти и сравнил их
с source bytes и независимыми эталонами. Volume hashes воспроизведены из seed,
ZIP/XLS originals сверены с зафиксированными hashes; XLS прочитан через xlrd.

- Small: 2 artifacts, 2 originals, 6335 B source, ZIP 1633 B — PASS.
- Volume: 8 artifacts, 7 originals, 7340032 B source; семь ZIP parts,
  крупнейшая 1049126 B — PASS.
- Partial: 2 artifacts, один original 703 B, ZIP 716 B; completed/complete=false
  и missing-control.bin с NOT_FOUND — ожидаемый PASS.
- Все client artifact hashes совпали с фактическим server scratch. Job/root IDs,
  complete, exact manifest selected set, included status, part/entry provenance,
  sizes/hashes и CRC сверены отдельно.
- Повторная client materialization имеет hashes и bytes действительно крупнейшей
  part; пользовательская evidence подтверждает отдельные tool calls. Собственный
  локальный verifier не выдаётся за повтор сетевой доставки в ChatGPT.
- Source до/после и при приёмке: 13 файлов, 10493671 B; tree SHA-256
  `9d4dbd5e58b17046eee6c3ae77a0deb19053ecac4397d434294e08a5feb8a86a`.
- Teardown: пользователь подтвердил Ctrl+C; сохранён GRACEFUL_STOP_PASS, forced=false.
  При независимой read-only проверке принадлежащий опыту PID отсутствует,
  loopback readyz недоступен. Старое зависание 003 здесь не воспроизвелось;
  устранение его причины не заявляется.

Клиентские verifier имеют ограничения: small допускает лишние manifest records,
small/volume не проверяют included status, volume допускает запасное поле completed
и повторяет заранее выбранную part. Для фактического опыта эти пробелы закрыты
независимой сверкой выше: manifest корректны, complete=true в positive jobs и
выбранная part действительно крупнейшая. Это не универсальный XLS parser или
измерение верхнего лимита ChatGPT. Client submit/fetch timings не представлены.

## Исправления интеграции

Runtime относительно reviewed/live SHA не изменялся. В новой тестовой утилите
scripts/bundle-check/live-mcp-capture.mjs найден возврат уже исправленного класса
R8: source canonical, но destinations проверялись лексически. При внешнем junction
или 8.3 alias внутрь source helper мог создать output в источнике. Неизменность
реального live source подтверждена отдельно, поэтому functional PASS сохраняется.

В интеграционной ветке перенесён принятый алгоритм canonical destination/nearest
existing ancestor; обе проверки выполнены до любых mkdir. Последующие записи и
FS_SNAPSHOT_DIR используют проверенные canonical paths. Добавлены 6 regression
тестов, 18 CLI-сценариев для обеих destinations: root/child/missing descendant,
canonical/junction/actual 8.3. Каждый проверяет source listing/bytes и отсутствие
внешнего output. До исправления: 2 PASS, 4 FAIL; после: 6 PASS, 0 FAIL, 0 skips.
Положительный stdio small capture и независимый ZIP/XLS verifier после fix — PASS.

Приёмка также воспроизвела Prettier FAIL в новом live-отчёте. Заявление исполнителя
о финальном static PASS этим не подтвердилось; отчёт отформатирован в интеграции.
Это документационный дефект, не ошибка live delivery. Форматировались только
изменённые документы/скрипты. Финальный независимый npm run check — PASS:
409 tests, 401 pass, 0 fail, 0 cancelled, 8 прежних platform/permission skips.
Build/types/ESLint/Prettier/knip прошли. Итог remote CI приведён ниже.

## CI и интеграция

[PR #5](https://github.com/obolibok/filesystem-mcp/pull/5) принят и слит в main:
[merge `9bec9f40`](https://github.com/obolibok/filesystem-mcp/commit/9bec9f4018587a28e488551b36dd273960a6ab65).
Проверенный head — `359cec25086e55f17f66a07a81f1c593fd860a2f`.
[CI run 35649089044](https://github.com/obolibok/filesystem-mcp/actions/runs/35649089044)
выполнил полный npm run check на обеих платформах:

| Платформа | Tests | Pass | Fail | Skipped |
| --------- | ----- | ---- | ---- | ------- |
| Windows   | 409   | 406  | 0    | 3       |
| Ubuntu    | 409   | 401  | 0    | 8       |

Новые проверки live helper прошли на Windows, включая настоящие junction и 8.3.
На Ubuntu прошёл POSIX FIFO; платформенные пропуски не считаются выполненными
сценариями. Code review, live, локальная интеграция и CI — PASS.

## Evidence и границы

Подробные локальные проверки лежат в ignored .tmp/004-live-acceptance,
.tmp/004-integration-local и .tmp/004-live-harness-*.log. Передача production
документов, restart tunnel и изменение профилей при приёмке не выполнялись.
В Git сохраняются обезличенные выводы, test helpers и regressions; архивы,
клиентские файлы, ключи и личные пути не включены.
