# 007: независимое review R1, 25.09.2026

Результат: **changes requested**. Проверенный commit
`aca33f39757a7a3dfd8b3b86cf6a90597a09705f`, base
`86b7eafc6be7f97cf1a0cdc013426c48ac402585`.
Diff: 10 файлов, +887/-42. Независимый detached checkout .worktrees/007-review;
checkout исполнителя не изменялся. Runtime пока не интегрирован.

## Проверки

Независимый npm ci --ignore-scripts и полный npm run check PASS:
427 tests, 419 pass, 0 fail, 8 platform/permission skips, Windows/Node 24.15.0.
Это не закрывает два дополнительных подтверждённых сценария ниже. Воспроизведение
на synthetic files, с настоящим guard; пользовательский диск не сканировался.

## R1 — P1: sensitive child теперь прерывает весь обход

Место: src/core/snapshot-pipeline.ts:135–157, особенно отказ для ACCESS_DENIED
на строке 153. Прежний recoverable ACCESS_DENIED исключён целиком. Но этим кодом
PathGuard обозначает не только смену общей политики, а обычный запрет sensitive
дочернего файла, например placeholder b.pem. Общая политика при этом не менялась.

Реальный GuardedFileSystem/PathGuard, source a.txt, b.pem, z.txt; в каждом файле
только synthetic текст, параметры includeHidden=false/includeIgnored=false:

| Версия        | State                     | Files | Inaccessible | Artifacts |
| ------------- | ------------------------- | ----: | -----------: | --------: |
| принятый main | completed, complete=false |     2 |            1 |         2 |
| aca33f39      | failed                    |     1 |            0 |         0 |

Stop reason нового head: Sensitive file blocked. Это регрессия прежних skip cases
из исходного большого опыта и блокирует исправление реального сценария.
Нужна явная разница между устойчивым исключением child и root/global policy
failure. Сохранять запрет чтения sensitive, продолжать доступных соседей;
добавить regression с настоящим guard, не только injected ACCESS_DENIED.

## R2 — P2: fatal path теряется при 8.3 scratch

Места: src/core/job-manager.ts:277 и :808. Диагностике передаётся
config.scratchDirectory, хотя initialize уже канонизировал scratch для реальных
операций. При 8.3 spelling в config и длинном canonical ctx.tempPath prefix-check
не узнаёт собственный scratch и выбрасывает известный допустимый path.

Подтверждение: настоящий 8.3 alias isolated scratch; producer бросает
FsError(IO_ERROR, synthetic message, ctx.tempPath, ENOSPC cause с syscall=write).
Итог failed, nativeErrorCode=ENOSPC сохранён, fatalError.path отсутствует.
Использовать канонический принадлежащий manager scratch для samples/fatal,
сохраняя запрет outside paths. Добавить regression на реальном Windows alias.

## Передача

Оба замечания с шагами воспроизведения отправлены существующему исполнителю
007, threadId `01a0d7d8-88f3-7480-84fe-7d4d1f436f6e`. Разрешено продолжить
исправления в исходной ветке/карточке и вернуть новый локальный head на повторное
review. Push/merge, обновление установленного комплекта и live опыт не выполнялись.
Локальные доказательства — ignored review scratch; этот документ самодостаточен.
